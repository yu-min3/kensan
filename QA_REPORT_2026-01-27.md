# Kensan アプリ QAテストレポート

**テスト日時**: 2026年1月27日
**テスト環境**: Chrome (localhost:5173)
**テスト担当**: Claude Code

---

## サマリー

| カテゴリ | 件数 |
|---------|------|
| 修正完了 | 3 |
| 追加調査必要 | 1 |
| 正常動作 | 10 |

---

## 修正状況

### ✅ 修正完了

#### 1. 学習記録編集画面で目標名がUUIDで表示される → **修正完了**

**場所**: `/notes/{id}` (学習記録編集画面)
**修正内容**: `NoteEditor.tsx`のSelect表示ロジックを修正。ヘルパー関数を追加してIDから名前を取得するように変更。
**修正ファイル**: `src/components/note/NoteEditor.tsx`

---

#### 2. 設定ページでサイドバーが非表示 → **修正完了**

**場所**: `/settings`
**修正内容**: `App.tsx`のルーティングを修正。設定ページをLayout内に配置し、初期設定用に別ルート`/settings/initial`を作成。
**修正ファイル**: `src/App.tsx`

---

#### 3. 週間学習時間グラフのY軸がマイナス値 → **修正完了**

**場所**: `/` (ダッシュボード)、`/analytics` (分析・レポート)
**修正内容**: RechartsのYAxisに`domain={[0, 'auto']}`と`allowDataOverflow`プロップを追加。
**修正ファイル**:
- `src/pages/S02_Dashboard.tsx`
- `src/pages/A01_AnalyticsReport.tsx`

---

### ⚠️ 追加調査必要

#### 4. クイックメモ保存後のフィードバックがない

**場所**: ダッシュボード右下のクイックメモウィジェット
**対応内容**:
- `FloatingMemoButton.tsx`に`toast.success/error`を追加
- `main.tsx`のToasterに`theme="dark"`を追加

**現在の状態**: コードは正しく追加されているが、トースト通知が画面に表示されない。
**推定原因**:
- z-indexの問題でトーストが他の要素の下に隠れている可能性
- sonnerの設定に追加の調整が必要な可能性

**修正ファイル**:
- `src/components/common/FloatingMemoButton.tsx`
- `src/main.tsx`

---

### ℹ️ 正常動作（元の調査で問題なし）

#### 5. ダッシュボード・分析ページのデータセクションが空

**場所**:
- `/` (ダッシュボード) - 「目標別 今週の進捗」セクション
- `/analytics` (分析・レポート) - 「目標別時間配分」など

**結論**: データがない場合の正常な表示状態。バックエンドに該当データが存在しない。

---

## 正常動作確認済み機能

| ページ/機能 | 状態 | 備考 |
|------------|------|------|
| サイドバーナビゲーション | ✅ 正常 | 全ページへの遷移確認済み |
| ダッシュボード基本表示 | ✅ 正常 | ウェルカムメッセージ、統計カード表示 |
| 設定ページのサイドバー | ✅ 修正済み | サイドバー表示確認 |
| デイリー画面 - タスクチェック | ✅ 正常 | チェック時にカウント更新 |
| デイリー画面 - 予定追加ダイアログ | ✅ 正常 | ダイアログ開閉、フォーム表示 |
| タスク管理 - 3カラムレイアウト | ✅ 正常 | 目標→マイルストーン→タスク表示 |
| ノート一覧 | ✅ 正常 | フィルター、検索ボックス表示 |
| ノート編集 - 目標選択 | ✅ 修正済み | 目標名が正しく表示 |
| 定期タスク | ✅ 正常 | タスク一覧、トグル表示 |
| AI振り返り | ✅ 正常 | レポート表示、生成ボタン |
| タイマー開始ダイアログ | ✅ 正常 | ダイアログ開閉、タスク選択UI |
| 週間学習時間グラフ | ✅ 修正済み | Y軸が0から開始 |

---

## 技術的な学び

### Rechartsのdomain設定

`domain={[0, 'auto']}`だけでは、Rechartsが自動的にパディングを追加してY軸に負の値が表示されることがある。`allowDataOverflow`プロップを追加することで、ドメイン範囲を厳密に適用できる。

```tsx
<YAxis domain={[0, 'auto']} allowDataOverflow fontSize={12} unit="h" />
```

### shadcn/ui Select表示

SelectValueのchildrenを直接設定する代わりに、SelectTrigger内にspanを配置して表示テキストを制御する方が確実。

```tsx
<SelectTrigger>
  <span className="truncate">
    {getDisplayName(value) || 'プレースホルダー'}
  </span>
</SelectTrigger>
```

---

## 残タスク

1. **要調査**: トースト通知が表示されない問題
   - sonnerのz-index設定確認
   - Toasterの位置調整
   - デバッグログの追加

---

## 付録：修正したファイル一覧

| ファイル | 修正内容 |
|----------|----------|
| `src/components/note/NoteEditor.tsx` | Select表示ロジック修正 |
| `src/App.tsx` | ルーティング修正（設定ページ） |
| `src/pages/S02_Dashboard.tsx` | YAxisにallowDataOverflow追加 |
| `src/pages/A01_AnalyticsReport.tsx` | YAxisにallowDataOverflow追加 |
| `src/components/common/FloatingMemoButton.tsx` | toast通知追加 |
| `src/main.tsx` | Toasterにtheme="dark"追加 |

---

## 追加テスト: タスク定期設定機能 (2026-01-27 午後)

### テスト概要

定期設定機能を含むフロントエンド全般の再テストを実施。

### 新機能: タスク定期設定

#### 確認した機能
- タスク編集ダイアログに「繰り返し（任意）」フィールドを確認
- 選択肢:
  - 繰り返しなし
  - 毎日
  - 毎週（平日のみ）
  - カスタム（曜日指定）
- カスタム選択時に曜日チェックボックス（日〜土）が表示される

#### 動作確認結果
- **正常動作**: ドロップダウン、曜日選択UI共に正常に動作

---

### 修正した問題

#### 1. [修正済] タスク/マイルストーン/目標ダイアログで500エラー

**画面**: `/tasks` (タスク編集ダイアログ等)
**原因**: `entity_memos` テーブルがDBに存在しなかった
**解決方法**: `backend/migrations/016_entity_memos.sql` を手動で適用
```bash
cat backend/migrations/016_entity_memos.sql | docker exec -i kensan-postgres psql -U kensan -d kensan
```
**結果**: entity-memos APIが200を返すようになり、ダイアログが正常に動作

---

### 追加で発見された問題点

#### 2. [軽微] ノート作成画面の日付フィールドにスピナーが継続表示

**画面**: `/notes/new`
**症状**: 日付フィールド（01/27/2026）の右側にローディングスピナーが表示され続ける
**影響**: 機能自体は使用可能だが、ユーザーに読み込み中の印象を与える
**再現手順**:
1. ノート画面で「新規作成」をクリック
2. 日付フィールドを確認

**推測される原因**: 日付関連のAPIコールまたは状態管理の問題

#### 2. [情報] タスク管理画面「今週の定期タスク」ウィジェットが空

**画面**: `/tasks`
**症状**: 右サイドバーの「今週の定期タスク」ウィジェットに「定期タスクがありません」と表示
**補足**: 定期タスク画面（`/routines`）には3件のルーチンタスクが存在する
**確認事項**: タスク管理画面の「今週の定期タスク」は、タスクの「繰り返し」設定に連動しているのか、それとも定期タスク（routine-service）に連動しているのかを確認する必要がある

---

### 全画面テスト結果

| 画面 | ステータス | 備考 |
|------|-----------|------|
| ダッシュボード | OK | 正常動作 |
| タスク管理 | OK | 定期設定機能を含め正常 |
| デイリー | OK | タイムブロック、タスク一覧正常 |
| 定期タスク | OK | 一覧表示、トグル正常 |
| ノート一覧 | OK | フィルター、検索正常 |
| ノート作成 | 要確認 | 日付フィールドにスピナー継続表示 |
| ノート編集 | OK | 学習記録の編集正常 |
| 分析・レポート | OK | グラフ表示正常 |
| AI振り返り | OK | レポート表示正常 |
| メモ機能 | OK | 一覧表示、追加UI正常 |
| 設定 | OK | タイムゾーン、テーマ設定正常 |

### コンソールエラー

**結果**: コンソールエラーなし

---

### 総評

フロントエンド全般は良好に動作しています。新しいタスク定期設定機能も正常に動作することを確認しました。ノート作成画面の日付フィールドのスピナー問題は軽微ですが、ユーザー体験の観点から修正が推奨されます。
