# 2026-01-22 エディタ実装 & アーキテクチャ議論

## 完了した作業

### 1. Markdownエディタ実装（TipTap）
- Milkdown → TipTap に変更（最も人気のあるエディタ）
- `src/components/editor/MarkdownEditor.tsx`
- 機能: 見出し、太字、斜体、取り消し線、リスト、引用、コードブロック、リンク、Undo/Redo
- tiptap-markdownでMarkdown形式での保存に対応

### 2. draw.ioエディタ実装
- react-drawioを使用
- `src/components/editor/DrawioEditor.tsx`
- 全画面モーダル対応（狭い埋め込みの問題を解決）
- プレビュー表示（SVG抽出）
- ESCキーで保存して閉じる

### 3. .gitignore更新
- Python仮想環境（kensan-ai/.venv/）を追加
- __pycache__、*.tsbuildinfo等を追加
- git候補が2K → 87件に削減

## 議論中: データ保存アーキテクチャ

### 現状
- content（Markdown/drawio XML）をDBのTEXTカラムに保存
- 画像対応なし

### ユーザー要件
- 画像も貼りたい
- 全文検索必要
- IndexedDBでローカルファースト検索を検討中
- AIサービス（kensan-ai）との連携

### 検討中の構成案

```
┌─────────────────────────────────────────────────────────┐
│  ブラウザ（ローカル）                                     │
│  ┌─────────────┐ ┌────────────┐                         │
│  │ IndexedDB   │ │ MiniSearch │                         │
│  │ - 全文保存   │ │ - 検索Index│                         │
│  └─────────────┘ └────────────┘                         │
└─────────────────────┬───────────────────────────────────┘
                      │ 同期/AI検索
┌─────────────────────▼───────────────────────────────────┐
│  サーバー                                                │
│  ┌──────────────┐  ┌─────────────┐  ┌────────────────┐  │
│  │ PostgreSQL   │  │ pgvector    │  │ S3/R2          │  │
│  │ (メタデータ?) │  │ (embedding) │  │ (画像ファイル)  │  │
│  └──────────────┘  └─────────────┘  └────────────────┘  │
│                           ↑                              │
│                    kensan-ai サービス                    │
└─────────────────────────────────────────────────────────┘
```

### 未決定事項（次回議論）
1. 全文をどこに持つか
   - IndexedDBのみ（プライバシー重視）
   - PostgreSQLにも同期（マルチデバイス対応）
2. pgvectorの使い方
   - embeddingだけ保存（全文なし）も可能
   - Supabase等マネージドサービスの検討
3. kensan-aiサービスとの連携方法
   - 検索APIの設計
   - embedding生成のタイミング

## 次回TODO
- [ ] 全体アーキテクチャの決定（kensan-aiサービスと合わせて）
- [ ] 画像アップロード機能の実装方針
- [ ] IndexedDB導入の検討
